tombstone.html.erb<%= f.input :resource_type, as: :select, collection: Hyrax::ResourceTypesService.select_options,
            selected: f.object.persisted? ? f.object.resource_type : 'Dataset',
            input_html: { class: 'form-control', multiple: true } %>